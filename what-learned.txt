1. localStorage.setItem() is used to set item on localStorage.
    ==> localStorage.setItem(key, value);
2. localStorage.getItem(key) is used to get item on localStorage.
    ==> localStorage.getItem(key);
3. sessionStorage.setItem() is used to get item on sessionStorage.
    ==> sessionStorage.setItem(key, value);