1. get route parameter by using useParams hook of react-router-dom;
    ==> import {useParams} from 'react-router-dom';
    function AppDetail() {
        const rParam = useParams();
        return(
            <h1>{rParam}</h1>
        );
    }
2. write if condition on react jsx code. By using (&&) operator. if the condition is true than render the part.
    ==> {props.showAddToCart && <button>Add to cart</button>}